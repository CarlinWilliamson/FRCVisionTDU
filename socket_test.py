import socketimport threadingwhile True:    try:        socket.send("1")        print("Sent: " + data)    except Exception, e:        print("Error sending data")        socket = None        try:            socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)            socket.settimeout(2)            socket.connect(("10.31.32.30", 1180))            socket.settimeout(5)            print("Control connection established")        except Exception, e:            print("Unable to connect to robot on control port")